version: 2.1
orbs:
  lucos: lucos/deploy@0
workflows:
  version: 2
  build-deploy:
    jobs:
      - lucos/component-npm-publish:
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/